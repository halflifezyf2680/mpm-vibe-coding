version: "1"

startCommand:
  type: stdio
  # Smithery CLI 以及底层的 Cursor 工具都能自动解析跨平台启动
  command: "mpm-bin/mpm-go"
  args: []

build:
  type: custom
  # 跨平台 Node 脚本，由于 Smithery 运行环境必备 Node，必定有效
  command: "node scripts/smithery-build.js"
